import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  CreateBoardButton: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    height: 80,

  },
  addIcon: {
    height: 35,
    width: 35,
  },
});
