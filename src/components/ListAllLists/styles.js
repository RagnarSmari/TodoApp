import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textStyle: {
    flexDirection: 'row',
    textAlign: 'center',
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 10,

  },
});
