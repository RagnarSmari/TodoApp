import { StyleSheet } from 'react-native';

StyleSheet.create({
  modal: {
    backgroundColor: '#000000',
  },
  modalView: {
    alignContent: 'center',
  },
  mView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 22,
  },
});
