import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    width: 180,
    height: 180,
    margin: 3.5,
    borderRadius: 20,
  },
  modalView: {
    backgroundColor: '#000',
  },
});
