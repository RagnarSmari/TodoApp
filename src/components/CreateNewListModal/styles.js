import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    flexDirection: 'row',
    height: 40,
    margin: 12,
    borderWidth: 1,
    borderRadius: 8,
    padding: 10,

  },
  container: {
    backgroundColor: 'white',
  },
});
